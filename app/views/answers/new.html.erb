<h1>New answer</h1>
<%= error_messages_for :answer %>

<% form_for([@question, @answer]) do |f| %>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', question_answers_path(@question) %>